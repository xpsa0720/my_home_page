import 'package:flutter/material.dart';

class PptPageComponent extends StatelessWidget {
  final Widget child;
  const PptPageComponent({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
